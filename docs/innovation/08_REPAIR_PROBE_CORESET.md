# Repair-Probe Coreset gate record

**Status: SKIPPED.** Prompt 9 requires a frozen selector that passed its source
gate. DCRG and CPI both received NO-GO, and Source Best Single is a baseline
rather than the proposed variable-pool selector whose onboarding policy this
experiment is intended to preserve.

Running probe selection now would optimize agreement with a failed or
post-selected policy and would not answer the registered hypothesis. No probe
lists, budget curves, or simulated onboarding claims were produced.

