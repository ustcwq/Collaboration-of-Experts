# Improve6 最终评分公式的必要项、可删项与简化方案

> 研究对象：Improve6 / RepairChain 最终评分公式  
>
> 原始公式：
>
> \[
> S_m^{\mathrm{RC}}(q)
> =
> 0.30L_m(q)
> +0.25H_m^{(1)}(q)
> +0.18H_m^{(2)}(q)
> +0.16A_m(q)
> +0.11G_m
> \]
>
> 目标：判断哪些项目可以简化省略，哪些项目是 Improve6 的核心；若只突出 Improve6，在效果有效的前提下，主公式最少应保留哪些项。
>
> 主要依据：
>
> - `IMPROVE5_6_REAL_EXAMPLE(1).md`
> - `ALL_EXPERIMENTS_SUMMARY(1).md`
> - `bench_coe_fcs.pdf`

---

# 一、结论先行

现有材料**没有逐项消融实验**，因此不能在经验上断言某个分量“一定必须”。当前文件只展示了完整五项公式在一个 MathVista 案例中能选到正确答案，而且这五个权重是人为预设的，并非训练得到。

来源：

- `IMPROVE5_6_REAL_EXAMPLE(1).md` 第 474–509 行；
- `IMPROVE5_6_REAL_EXAMPLE(1).md` 第 755–804 行。

但从方法结构、信息重复和 Improve6 的创新定位看，可以给出比较明确的判断：

- **真正属于 Improve6 核心、原则上必须保留的是 \(H_m^{(1)}(q)\)**；
- **最应该从主公式中删除的是 \(L_m(q)\)**；
- **\(H_m^{(2)}(q)\) 只有在消融证明稳定有效时才保留**；
- **\(A_m(q)\) 建议从直接加权项改为答案簇支持、门控条件或平局处理**；
- **\(G_m\) 建议从最终分数删除，只保留在纠错矩阵的平滑先验中，或作为最终 tie-breaker**。

最推荐的简化路线是：

\[
\boxed{
S_m^{\mathrm{core}}(q)=H_m^{(1)}(q)
}
\]

并使用：

\[
A_m(q)\rightarrow G_m
\]

作为平分或低置信度情况下的依次回退规则。

若担心纯一跳图不够稳定，则使用最小两项版本：

\[
\boxed{
S_m(q)=
\beta H_m^{(1)}(q)
+
(1-\beta)A_m(q)
}
\]

但更合理的是把它改成**答案簇级评分**，而不是继续在给出相同答案的专家之间排序。

---

# 二、五个分量分别是否应该保留

| 分量 | 是否属于 Improve6 核心 | 信息重复情况 | 建议 |
|---|---:|---:|---|
| \(L_m(q)\) | 否 | 与 Improve5、MCB/KNOP 局部能力估计重叠 | **从主公式删除** |
| \(H_m^{(1)}(q)\) | 是 | 不可由其他项替代 | **必须保留** |
| \(H_m^{(2)}(q)\) | 部分 | 是 \(H^{(1)}\) 经同一 \(C\) 再传播的派生项 | **默认删除，证明增益后再加** |
| \(A_m(q)\) | 不是核心，但有实用价值 | 已经进入失败权重 \(\omega(q)\)，存在部分重复 | **不必直接加权，可作为答案簇支持或 tie-breaker** |
| \(G_m\) | 否 | 已经进入 \(C_{ij}\) 的平滑，存在明显重复 | **删除直接项，只保留为先验或 tie-breaker** |

---

# 三、为什么 \(L_m(q)\) 最应该删除

\(L_m(q)\) 是基于相似历史输出状态的 KNN 局部成功率：

\[
L_m(q)=
\sum_{r\in\mathcal N_k(q)}
\alpha_r(q)Y_{r,m}.
\]

它回答的是：

> 在与当前输出状态相似的历史问题中，哪个专家更容易答对？

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 161–195 行。

这是 Improve5 的主要机制，而不是 Improve6 的独特机制。

Improve6 真正想回答的是：

> 当前可能失败的专家出现后，哪个专家历史上最擅长修复这些失败？

原始案例文件也明确区分了两者：

- Improve5 主要依赖局部历史可靠性；
- Improve6 主要依赖一跳和两跳纠错关系。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 820–837 行。

因此，如果主公式仍然给 \(L_m(q)\) 最大权重 \(0.30\)，审稿人很容易提出：

> 结果究竟来自新的纠错图，还是来自已有的 output-profile KNN？

此前的查新已经表明，\(L_m(q)\) 对应的 output-profile dynamic selection 与 MCB、KNOP 高度重叠。保留它不仅让公式复杂，还会明显削弱 Improve6 的新颖性归因。

## 建议

将 \(L_m(q)\) 从 Improve6 主方法中完全删除：

\[
S_m(q)
=
\alpha H_m^{(1)}(q)
+\beta H_m^{(2)}(q)
+\gamma A_m(q)
+\delta G_m.
\]

然后把包含 \(L_m(q)\) 的版本命名为：

- `RepairGraph + Local-KNN`；
- `Improve6 + Improve5 hybrid`。

该版本只作为增强版或消融，而不是主方法。

---

# 四、为什么 \(H_m^{(1)}(q)\) 是唯一真正必须的项

一跳分数定义为：

\[
\mathbf H^{(1)}(q)=\boldsymbol\omega(q)C,
\]

即：

\[
H_j^{(1)}(q)
=
\sum_{i=1}^{M}
\omega_i(q)C_{i,j}.
\]

它将以下两类信息直接结合：

1. 当前 Query 上哪些专家看起来可能失败；
2. 源域中“专家 \(i\) 错时，专家 \(j\) 能否修复”。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 396–423 行。

这是 Improve6 与以下方法区别最大的部分：

- 普通多数投票；
- 单模型全局准确率；
- Improve5 的 KNN 局部能力；
- 普通 query router。

删除 \(H^{(1)}\) 后，方法就失去了“失败条件纠错图”的核心。

## 当前真实案例也支持这一点

对 MathVista 第 85 题：

| 专家 | \(H^{(1)}\) |
|---|---:|
| Qwen3-VL | 0.238 |
| LFM2.5-VL | 0.209 |

Qwen 的一跳修复证据明显更高。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 725–737 行。

根据文件给出的全部分量，可以计算 Qwen 相对 LFM 的加权优势：

| 项 | Qwen−LFM 原始差值 | 加权贡献 |
|---|---:|---:|
| \(0.30L\) | \(0.509-0.539=-0.030\) | **−0.00900** |
| \(0.25H^{(1)}\) | \(0.238-0.209=0.029\) | **+0.00725** |
| \(0.18H^{(2)}\) | \(0.194-0.175=0.019\) | +0.00342 |
| \(0.16A\) | \(0.455-0.455=0\) | 0 |
| \(0.11G\) | \(0.298-0.264=0.034\) | +0.00374 |

总差值约为：

\[
-0.00900+0.00725+0.00342+0+0.00374
=
0.00541,
\]

与文档中约 \(0.353-0.347\) 的差距一致。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 755–804 行。

在这一个案例中：

- 删除 \(H^{(1)}\) 后，Qwen 会被 LFM 反超；
- 删除 \(H^{(2)}\) 后，Qwen 仍然略胜；
- 删除 \(A\) 后，Qwen 仍然胜；
- 删除 \(G\) 后，Qwen 仍然胜；
- 删除 \(L\) 后，Qwen 的领先反而扩大。

这只是一道题，不能代替完整消融，但它至少说明：

> **在已展示案例里，真正决定 Improve6 选择结果的是 \(H^{(1)}\)，而不是 \(L\)、\(A\) 或 \(G\)。**

---

# 五、\(H_m^{(2)}(q)\) 为什么可以优先省略

两跳分数为：

\[
\mathbf H^{(2)}(q)
=
\widehat{\mathbf H}^{(1)}(q)C.
\]

它本质上是：

\[
H^{(1)}
\rightarrow
\text{归一化}
\rightarrow
\text{再乘同一个 }C.
\]

因此，\(H^{(2)}\) 并不是独立证据，而是 \(H^{(1)}\) 和 \(C\) 的确定性派生量。

文件也说明，两跳不表示模型依次调用或真实修复链；所有候选答案早已生成。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 425–472 行。

它面临三个问题：

1. **和 \(H^{(1)}\) 高度相关**；
2. **可能主要放大图中的高入度强模型**；
3. **可能发生过平滑，让不同 Query 的选择逐渐趋同**。

在示例中：

- Qwen 的 \(H^{(2)}=0.194\)；
- LFM 的 \(H^{(2)}=0.175\)。

但删除这一项，Qwen 仍然保持领先。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 739–753 行。

## 判断标准

只有满足以下条件，才值得保留 \(H^{(2)}\)：

\[
Acc(H^{(1)}+H^{(2)})
>
Acc(H^{(1)})
\]

并且该增益必须：

- 在多个数据集上同方向；
- 在专家池替换后仍存在；
- 优于随机图、对称图和静态中心性；
- 不是只在已经看过的 MathVista 上成立；
- 不造成 worst-domain 明显下降。

否则应直接删除 \(H^{(2)}\)。

## 命名上的影响

如果删除 \(H^{(2)}\)，不应继续叫 **RepairChain**，因为已经没有“链式传播”。

更合适的命名是：

- **Failure-Conditioned Repair Graph，FCRG**；
- **Directed Expert Rescue Graph，DERG**；
- **Failure-Conditioned Rescue Selection，FCRS**。

---

# 六、\(A_m(q)\) 可以从最终公式删除，但不建议从整个方法删除

答案支持率为：

\[
A_m(q)
=
\frac{1}{M}
\sum_l
\mathbf 1[a_l(q)=a_m(q)].
\]

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 82–123 行。

它已经进入失败信号：

\[
F_m(q)
=
\operatorname{clip}
\left(
1-A_m(q)+0.40U_m(q),0,1.6
\right),
\]

并进一步进入：

\[
\omega_m(q)
=
\frac{F_m(q)}{\sum_lF_l(q)}.
\]

所以 \(H^{(1)}\) 和 \(H^{(2)}\) 已经间接依赖 \(A_m(q)\)。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 338–393 行。

因此，最终再加：

\[
+0.16A_m(q)
\]

存在一定程度的重复使用。

不过这不是完全重复：

- \(A_i\) 在 \(\omega_i\) 中主要用于判断“哪个源专家可能失败”；
- 最终的 \(A_m\) 则直接奖励“候选专家自己的答案获得多少支持”。

所以 \(A\) 可以作为防止纠错图选择孤立答案的保守项，但不必一定作为连续加权项。

## 更好的处理方式

### 方式一：作为 tie-breaker

\[
m^*
=
\arg\max_m H_m^{(1)}(q),
\]

当两个专家的 \(H^{(1)}\) 差距小于阈值时，再比较：

\[
A_m(q),
\]

最后才比较 \(G_m\)。

### 方式二：改到答案簇层面

对于答案簇 \(c\)：

\[
\mathcal M_c(q)
=
\{m:a_m(q)=c\},
\]

定义：

\[
A_c(q)
=
\frac{|\mathcal M_c(q)|}{M}.
\]

这比在专家层面重复添加 \(A_m\) 更合理。

因为给出相同答案的专家，其 \(A_m\) 完全相同。在示例中，Qwen 和 LFM 都回答 2，二者：

\[
A_{\mathrm{Qwen}}=A_{\mathrm{LFM}}=0.455,
\]

所以 \(A\) 根本无法区分二者。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 17–25 行。

更本质地说：

> 最终准确率只关心选择了答案 2、3 还是 1，而不关心答案 2 是由 Qwen 还是 LFM 提供。

因此，答案支持率应当是**答案簇特征**，而不是专家区分特征。

---

# 七、\(G_m\) 最适合从最终公式中删除

全局准确率为：

\[
G_m
=
\frac{1}{N_s}
\sum_rY_{r,m}.
\]

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 141–159 行。

但 \(G_j\) 已经进入纠错矩阵：

\[
C_{i,j}
=
\frac{
\sum_{r:Y_{r,i}=0}Y_{r,j}+4G_j
}{
N_i^{\mathrm{fail}}+4
}.
\]

也就是说，某个专家 \(j\) 的全局准确率越高，它的所有入边已经会通过平滑先验被提高。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 295–336 行。

最终再加入：

\[
+0.11G_m
\]

相当于第二次奖励全局强专家。

这尤其值得警惕，因为扩展实验已经发现：

- query 路由容易塌缩到强单模型；
- 多数 OOD 结果仍低于最佳单模型；
- 替换强专家后方法性能明显变化。

来源：`ALL_EXPERIMENTS_SUMMARY(1).md` 第 368–376 行。

所以直接 \(G_m\) 可能重新引入原来要解决的“强模型支配”问题。

## 建议

保留 \(G_j\) 的一种用途即可。

### 方案 A：只保留在 \(C_{ij}\) 的平滑先验中

最终公式不再出现 \(G_m\)。

### 方案 B：只作为 tie-breaker

当其他分数完全相同或非常接近时，选择 \(G_m\) 更高的专家。

### 方案 C：低支持边的 fallback

当：

\[
N_i^{\mathrm{fail}}
\]

过小、纠错边不可靠时，才回退到 \(G_m\)。

不建议既把它放进 \(C\)，又在最终公式中再加一次。

---

# 八、如果只突出 Improve6，推荐保留哪些项

## 8.1 方案一：最纯粹的 Improve6 核心版

\[
\boxed{
S_m^{\mathrm{FCRG}}(q)
=
H_m^{(1)}(q)
}
\]

其中：

\[
\mathbf H^{(1)}(q)
=
\boldsymbol\omega(q)C.
\]

最终：

\[
m^*(q)
=
\arg\max_mH_m^{(1)}(q).
\]

平分规则：

1. 比较 \(A_m(q)\)；
2. 再比较 \(G_m\)；
3. 再按固定专家 ID，保证可复现。

### 优点

- 主公式只有一个项；
- 不存在五个手工权重；
- 创新归因最清楚；
- 与 Improve5 的 KNN 彻底分离；
- 最容易做因果消融；
- 在当前展示案例中仍会选择 Qwen。

### 缺点

- 对纠错矩阵噪声更敏感；
- 可能选到支持率很低的孤立答案；
- 必须检查图中心性和边统计可靠性。

这是**最适合作为论文主方法**的版本。

---

## 8.2 方案二：最推荐的实用简化版

\[
\boxed{
S_m(q)
=
\beta H_m^{(1)}(q)
+
(1-\beta)A_m(q)
}
\]

只保留：

- 一跳修复证据；
- 当前答案支持。

删除：

- \(L_m\)；
- \(H_m^{(2)}\)；
- 直接 \(G_m\)。

\(G_m\) 只保留在 \(C\) 的平滑中或 tie-breaker 中。

### 为什么这是最可能兼顾效果和清晰度的版本

\(H^{(1)}\) 提供 Improve6 的独特信息：

> 谁能够修复当前可能失败的专家？

\(A\) 提供一个非常简单的目标域无标签证据：

> 这个答案是否获得当前专家池的支持？

二者分别代表：

- 历史失败互补性；
- 当前 Query 的回答共识。

公式只剩一个自由权重 \(\beta\)，比原来的五项固定加权更容易解释和验证。

\(\beta\) 不应在目标数据集上选择，而应在以下环境中固定：

- 源域验证集；
- leave-one-subject-out；
- leave-one-source-benchmark-out。

---

## 8.3 方案三：更合理的答案簇级版本

这是最建议最终实现的结构：

\[
\bar H_c^{(1)}(q)
=
\frac{1}{|\mathcal M_c(q)|}
\sum_{m\in\mathcal M_c(q)}
H_m^{(1)}(q),
\]

\[
A_c(q)
=
\frac{|\mathcal M_c(q)|}{M},
\]

然后：

\[
\boxed{
S_c(q)
=
\beta\bar H_c^{(1)}(q)
+
(1-\beta)A_c(q)
}
\]

最终选择：

\[
c^*(q)=\arg\max_cS_c(q).
\]

### 为什么比专家级公式更合理

在真实案例中：

- Improve5 选择 LFM；
- Improve6 选择 Qwen；
- 两者都输出 2；
- 最终准确率完全相同。

来源：`IMPROVE5_6_REAL_EXAMPLE(1).md` 第 820–828 行。

所以把大量公式用于区分 LFM 和 Qwen，实际上并不能增加这道题的最终准确率。

真正需要区分的是：

| 答案簇 | 支持模型 |
|---|---|
| 2 | LFM、Qwen、Qwen3.5、Gemma 等 |
| 3 | InternVL 等 |
| 1 | MiniCPM 等 |

因此，论文任务最好从：

> 选择哪个专家

调整为：

> 选择哪个候选答案簇。

这也更符合 Improve5/6 已经属于 post-hoc response selection 的实际流程，而不是原论文中每题只激活一个专家的 single-inference 路由。

原 Bench-CoE 的受控协议明确规定每个 Query 只运行一个专家。

来源：`bench_coe_fcs.pdf` 第 5 页，解析文本第 565–574 行。

---

## 8.4 方案四：如果必须保留“RepairChain”两跳叙事

如果论文必须强调一跳和两跳，则可以使用：

\[
\boxed{
S_m(q)
=
\alpha H_m^{(1)}(q)
+
(1-\alpha)H_m^{(2)}(q)
}
\]

并把：

- \(A_m\) 作为 failure weight 和 tie-breaker；
- \(G_m\) 只作为 \(C\) 的平滑先验；
- \(L_m\) 完全删除。

这比原始五项公式更能突出 RepairChain。

但前提是：

\[
H^{(1)}+H^{(2)}
\]

必须稳定优于：

\[
H^{(1)}.
\]

否则没有必要保留两跳，也不应继续使用 “Chain” 命名。

---

# 九、推荐的删减顺序

不要一次把五项全部改掉。按照下面顺序做消融，能够明确判断每项作用。

| 实验编号 | 公式 | 目的 |
|---|---|---|
| M0 | \(L+H^{(1)}+H^{(2)}+A+G\) | 当前完整版本 |
| M1 | \(H^{(1)}+H^{(2)}+A+G\) | 检查是否能删除 Improve5 的 \(L\) |
| M2 | \(H^{(1)}+H^{(2)}+A\) | 检查直接 \(G\) 是否重复 |
| M3 | \(H^{(1)}+A\) | 检查两跳是否必要 |
| M4 | \(H^{(1)}\) | 纯 Improve6 核心 |
| M5 | \(H^{(1)}+H^{(2)}\) | 纯 RepairChain |
| M6 | \(A\) only | 多数/支持率基线 |
| M7 | \(L\) only | Improve5/KNOP 类基线 |
| M8 | \(G\) only | 全局最强专家基线 |

最关键的比较不是完整方法对最佳单模型，而是：

\[
M4\text{：}H^{(1)}
\quad\text{vs.}\quad
M5\text{：}H^{(1)}+H^{(2)}
\quad\text{vs.}\quad
M3\text{：}H^{(1)}+A.
\]

如果：

\[
H^{(1)}+A
\]

和完整五项公式基本相当，就应使用两项版本。

如果：

\[
H^{(1)}
\]

已经达到相同性能，就应使用一项版本。

---

# 十、如何判断一个项是否真的值得保留

不要仅看平均准确率。每增加一个分量，都应满足：

1. 在 source validation 或 source leave-one-environment-out 上有增益；
2. 在多个目标数据集上方向一致；
3. paired bootstrap 的置信区间支持正增益；
4. rescue rate 增加不能以更大的 harm rate 为代价；
5. worst-domain 不明显下降；
6. 专家池替换后仍有效；
7. 不依赖某一个模型或某一个答案格式；
8. 不能只在已经用于方法筛选的 MathVista 上有效。

尤其是 \(H^{(2)}\)，还必须比较：

- 真实图；
- 随机边权图；
- 对称化图；
- 去自环图；
- 只使用列均值的图中心性基线。

否则两跳增益可能只是强专家中心性，不是“间接修复链”。

---

# 十一、最终建议

## 11.1 论文主公式

优先使用：

\[
\boxed{
S_m(q)=H_m^{(1)}(q)
}
\]

配合：

\[
A_m(q)\rightarrow G_m
\]

作为 tie-breaker。

此时建议将方法重命名为：

> **Failure-Conditioned Repair Graph**

而不是 RepairChain。

## 11.2 若一跳版本稳定性不足

使用：

\[
\boxed{
S_m(q)
=
\beta H_m^{(1)}(q)
+
(1-\beta)A_m(q)
}
\]

并将 \(G_m\) 只放在 \(C_{ij}\) 的平滑先验中。

## 11.3 若必须保持两跳主张

使用：

\[
\boxed{
S_m(q)
=
\alpha H_m^{(1)}(q)
+
(1-\alpha)H_m^{(2)}(q)
}
\]

但只有在两跳相对一跳存在跨数据集稳定增益时才保留。

## 11.4 不建议继续使用

\[
0.30L
+0.25H^{(1)}
+0.18H^{(2)}
+0.16A
+0.11G.
\]

它的问题是：

- \(L\) 属于 Improve5；
- \(H^{(2)}\) 未证明有独立贡献；
- \(A\) 已经进入失败信号；
- \(G\) 已经进入纠错矩阵平滑；
- 五个系数都是手工设定；
- 机制归因不清；
- 审稿人很容易认为这是多个启发式分数的堆叠。

**最推荐的最终选择是：删除 \(L\)、删除直接 \(G\)、默认删除 \(H^{(2)}\)，保留 \(H^{(1)}\)，并将 \(A\) 改成答案簇级支持或 tie-breaker。**
