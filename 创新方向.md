# 一、先确定创新方向：不要再做一个新的固定权重评分公式

当前结果已经把核心问题暴露得很清楚：

- 路由类别准确率较高，并不等于最终系统准确率提高；
- 多数 OOD 设置下，Bench-CoE 仍低于最佳单模型；
- query router 容易塌缩到强模型；
- 更换一个专家后，最佳单模型、oracle 和路由性能都会明显变化。
- 原 Subject 路由只需更新 subject-expert mapping，但 Query 路由在专家池变化后需要重建标签和训练分类头。

Improve5 和 Improve6 已经不再是原论文中“先选一个专家、再只运行该专家”的单次推理模式。原论文明确规定每道题只激活一个专家；Improve6 则是在全部候选回答已经生成之后进行统计传播。

因此，后续创新最好明确分成两条路线：

1. **准确率路线**：利用已经生成的多专家回答，研究 post-hoc response selection；
2. **成本路线**：先运行少量模型，再按需追加专家，研究 sequential acquisition。

不建议继续提出类似：

[
S=0.31L+0.24H_1+0.19H_2+0.16A+0.10G
]

这样的 Improve7。Improve5 和 Improve6 已经使用人为固定系数，而且这些系数并非通过反向传播或严格统计推导得到。 新方法应当解决一个明确的统计问题，而不是再增加评分项。

------

# 二、近期文献已经封闭的方向

截至 **2026年8月8日**，下面几类创新表述已经不安全：

| 宽泛方向                 | 不能再主张的内容                         |
| ------------------------ | ---------------------------------------- |
| KNN 历史状态检索         | 首次使用相似历史 Query 预测模型表现      |
| rescue/harm              | 首次以“救对多少、改错多少”衡量集成收益   |
| higher-order aggregation | 首次使用模型相关性或高阶信息突破多数投票 |
| 图传播                   | 首次以图或消息传播聚合多模型回答         |
| conformal routing        | 首次为 LLM 路由提供风险控制              |
| unseen-model routing     | 首次支持新增模型而不重训                 |
| sequential routing       | 首次逐个调用模型并动态停止               |

例如，ContextualRouter 已经表明，检索相似历史问题并做 KNN 平均可以成为很强的泛化路由器，甚至能适应未见模型。([ACL Anthology](https://aclanthology.org/2026.eacl-srw.22.pdf))

2026 年 7 月的工作已经将两模型集成增益精确分解为 rescue mass 和 damage mass；2026 年 8 月的跨领域预印本也已经明确使用 primary-relative rescue/harm residual utility，并设置“保留主模型”动作。([arXiv](https://arxiv.org/html/2607.17384v1))

近期研究也已经利用一阶、二阶相关信息进行 LLM 答案聚合，并出现了基于 factor graph 和 structured message passing 的多 LLM 聚合方法。([arXiv](https://arxiv.org/html/2510.01499v1))

此外，风险控制路由、未知源域混合下的路由，以及通过模型表示接入未见模型，也都已有明确工作。([ACL Anthology](https://aclanthology.org/2026.acl-srw.70.pdf))

因此，下面提供的不是“绝对无人做过”的保证，而是经过上述文献边界收缩后的、**仍具有组合级创新潜力的研究假设**。任何方法一旦实验有效，还需要再进行一次逐 Claim 的敌对式查新。

------

# 三、推荐的创新思路总览

| 优先级 | 暂定名称                           | 核心问题                                          | 仅用现有缓存能否初验 | 新颖性潜力         |
| ------ | ---------------------------------- | ------------------------------------------------- | -------------------- | ------------------ |
| **S1** | DCRG：去混杂稳定纠错图             | Improve6 的边是否只是强模型和题目难度造成的假互补 | 是                   | 中高               |
| **S2** | CPI-Selector：模型池干预不变选择器 | 专家增删、复制、替换后能否保持稳定                | 是                   | 较高               |
| **S3** | TopoMix：目标输出拓扑迁移          | 如何不用目标标签判断哪个源 benchmark 更适合目标域 | 是，前提是有多个源域 | 较高               |
| A1     | Latent Failure Archetypes          | 能否用少量可解释失败状态替代 KNN                  | 是                   | 中等               |
| A2     | Repair-Probe Coreset               | 新专家是否只需少量 benchmark 探针即可接入         | 是                   | 中高               |
| A3     | Higher-order Rescue Motifs         | 哪些专家联合分歧模式预示少数答案正确              | 是                   | 中等、重叠风险较高 |
| B      | Sequential Rescue Acquisition      | 能否不运行全部模型仍接近全专家选择准确率          | 可离线模拟           | 中等、领域较拥挤   |

最建议先验证 **DCRG、CPI-Selector、TopoMix**，不要一开始把所有模块合成一个复杂系统。

------

# 四、思路一：DCRG——去混杂、跨环境稳定的纠错图

## 4.1 Improve6 当前最可能的问题

Improve6 使用：

[
C_{ij}=P(j\text{正确}\mid i\text{错误})
]

的平滑估计。

但这个值可能同时受三种因素影响：

1. 专家 (j) 本身全局就很强；
2. 专家 (i) 的错误样本中包含很多容易被其他模型解决的题；
3. 两个模型只是共享某些题目难度结构，而不是真正存在独特救援关系。

例如一个全局最强专家可能对几乎所有 (i) 都有较大 (C_{ij})，于是它成为纠错图中的高入度中心。此时一跳、两跳传播可能只是再次选择强单模型，而不是识别可迁移的纠错结构。

## 4.2 新方法

先估计专家 (j) 在每道源域题上的“应有正确率”：

# [ \widehat p_{rj}

P\left(
Y_{rj}=1
\mid
\text{difficulty}_r,,
\text{subject}_r,,
\text{query features}_r
\right).
]

使用严格 out-of-fold 方式计算残差：

[
E_{rj}=Y_{rj}-\widehat p_{rj}.
]

再定义环境 (e) 内的去混杂救援边：

# [ R^{(e)}_{ij}

\mathbb E
\left[
E_{rj}
\mid
Y_{ri}=0,\ e
\right].
]

解释是：

> 当 (i) 失败时，(j) 是否比“根据自身能力和题目难度本来应该达到的正确率”表现得更好。

因此：

- (R_{ij}>0)：存在超出强模型基线的额外救援能力；
- (R_{ij}\approx0)：原来的高 (C_{ij}) 主要由 (j) 本身较强造成；
- (R_{ij}<0)：当 (i) 失败时，(j) 也比预期更容易失败。

然后只保留跨环境稳定的正边：

# [ \widetilde R_{ij}

\max
\left(
Q_{0.1}
\left{
\operatorname{LCB}
\left(R^{(e)}*{ij}\right)
\right}*{e=1}^{E},
0
\right).
]

环境可以是：

- 不同 benchmark；
- 不同 subject；
- 不同数据来源；
- 或至少是预先固定的 source folds。

当前 Query 上，不再使用手工失败公式直接传播，而是训练一个 out-of-fold 失败估计器：

# [ \widehat f_i(q)

P\left(
Y_i=0
\mid
\text{当前答案拓扑、支持率、不确定性、模型指纹}
\right).
]

最终得到：

# [ H_j(q)

\sum_i
\widehat f_i(q)\widetilde R_{ij}.
]

## 4.3 与 Improve6 的实质区别

| Improve6           | DCRG                               |
| ------------------ | ---------------------------------- |
| 原始条件正确率     | 难度和全局能力调整后的残差救援     |
| 所有边均参与       | 只保留统计显著且跨环境稳定的正边   |
| 手工构造失败信号   | out-of-fold 学习当前失败概率       |
| 一跳、两跳固定传播 | 首先只做可解释的一跳残差传播       |
| 模型级选取         | 可进一步改为答案簇级选取           |
| 固定加权系数       | 通过源域验证学习或直接使用概率模型 |

## 4.4 关键消融

必须比较：

[
C_{ij}
\quad\text{vs.}\quad
C_{ij}-G_j
\quad\text{vs.}\quad
R_{ij}
\quad\text{vs.}\quad
\widetilde R_{ij}.
]

并测试：

- pooled edge 与 stable edge；
- 是否调整题目难度；
- 是否调整专家全局能力；
- 是否去掉自环；
- 一跳与两跳；
- 真实边与按度数保持的随机边；
- subject 作为环境与 benchmark 作为环境。

## 4.5 适合作为论文 Claim 的表述

较安全的表述是：

> We estimate directed rescue relations after removing the effects of expert strength and item difficulty, and retain only relations that remain positive across source environments.

不能声称“首次使用失败条件互补性”，因为这一基础思想已经存在。

------

# 五、思路二：CPI-Selector——通过模型池干预训练可变专家池选择器

这是我认为**最值得优先验证的方向**。

## 5.1 它针对的真实问题

当前方法严重依赖固定的专家维度：

- (Y\in{0,1}^{N_s\times M})；
- (C\in\mathbb R^{M\times M})；
- 当前回答状态也是一个固定 (M) 维结构。

新增、删除、替换专家后，这些对象都发生变化。

实验中，Qwen3.5-2B 被 Qwen3-VL-2B-Instruct 替换后：

- 最佳单模型从 17.01% 降至 14.69%；
- oracle 从 31.19% 降至 29.12%；
- 统一 Qwen3-VL Bench-CoE 下降到 12.63%。

而最新文献已经表明，同一模型家族的相关错误会明显降低集成的有效独立投票数，因此简单的 family weighting 本身也不能作为新贡献。([arXiv](https://arxiv.org/abs/2603.17111))

真正值得研究的是：

> 能否通过训练时主动改变专家池，使选择器学会对专家顺序、数量、同族复制、缺失输出和模型替换保持稳定。

## 5.2 不再选择专家，而是选择答案簇

对于当前 Query，将等价答案组成答案簇：

# [ \mathcal C(q)

\left{
c_1,c_2,\ldots,c_K
\right}.
]

建立一个二部结构：

[
\text{专家节点}
\longleftrightarrow
\text{答案簇节点}.
]

每个专家节点特征包括：

[
x_m=
[
\text{benchmark fingerprint},
\text{family},
G_m,
U_m(q),
\text{validity},
\text{cost},
\text{repair embedding}
].
]

每个专家通过一条边连接到它当前回答所属的答案簇。

对答案簇 (c) 使用 permutation-invariant pooling：

# [ h_c

\rho
\left(
\sum_{m:a_m(q)\in c}
\phi(x_m)
\right),
]

全局专家池表示为：

# [ h_{\mathrm{pool}}

\eta
\left(
\sum_m \psi(x_m)
\right).
]

最终：

# [ s_c

f(h_c,h_{\mathrm{pool}}).
]

选择：

[
c^*=\arg\max_c s_c.
]

最后只需要在答案簇 (c^*) 内选择任意一个提供该答案的专家输出。

这比“为每个专家评分”更合理，因为本例中 LFM、Qwen3-VL、Qwen3.5、Gemma 等五个专家都回答 2；真正需要判断的是“答案 2 是否正确”，而不是它们中哪一个专家的分数高 0.001。

## 5.3 核心创新：模型池反事实干预

训练时，对同一道源域题随机构造多个专家池：

### 干预一：专家 dropout

随机删除部分专家：

[
S'\subset S.
]

让模型学习在不同池规模下进行答案选择。

### 干预二：family dropout

一次删除整个模型家族，测试方法是否依赖某个家族。

### 干预三：clone duplication

复制一个专家的回答和静态指纹，构造一个虚拟同源模型。

复制一个完全相同的专家不应增加独立证据，因此要求：

[
P(c\mid S)
\approx
P(c\mid S+\operatorname{clone}(m)).
]

可以加入：

# [ \mathcal L_{\mathrm{clone}}

D_{\mathrm{JS}}
\left(
P(\cdot\mid S),
P(\cdot\mid S+\operatorname{clone}(m))
\right).
]

### 干预四：缺失输出

模拟：

- judge 缺失；
- answer extraction 失败；
- 某个模型不可用；
- 无效答案。

### 干预五：专家替换 episode

训练 episode 中故意隐藏一个专家或整个家族，然后测试：

- leave-one-expert-out；
- leave-one-family-out；
- Qwen3.5/Qwen3-VL swap；
- 原 6 专家池与 judged4 池。

## 5.4 方法真正可能成立的新颖点

不是“使用图聚合回答”，因为 structured message passing 已经存在。([arXiv](https://arxiv.org/abs/2606.00405))

更窄的新颖点是：

> Counterfactual model-pool interventions are used to train a permutation-invariant post-hoc answer-cluster selector that remains stable under expert addition, removal, duplication, family shift, and missing outputs.

即：

> 通过反事实模型池干预，训练对专家增删、复制、家族变化和缺失输出具有鲁棒性的答案簇选择器。

## 5.5 必须做的性质测试

该方法除了准确率，还应验证以下数学或程序性质：

| 测试                             | 期望                    |
| -------------------------------- | ----------------------- |
| 专家顺序随机打乱                 | 输出完全不变            |
| 复制一个完全相同专家             | 输出近似不变            |
| 删除一个无效专家                 | 输出不变                |
| 随机减少 20% 专家                | 性能缓慢下降            |
| 删除一个模型家族                 | 优于未做干预训练的模型  |
| Qwen3.5→Qwen3-VL 替换            | 性能退化小于 Improve5/6 |
| 不同专家池数量                   | 无需修改输出层维度      |
| 新专家仅有 benchmark fingerprint | 无需重新训练整个选择器  |

------

# 六、思路三：TopoMix——根据无标签目标回答拓扑选择源 benchmark

## 6.1 当前方法的隐含假设

Improve5 和 Improve6 都默认：

> 源域 MMMU-Pro 中学到的局部状态、模型可靠性和纠错关系，可以直接迁移到 MathVista。

但实际结果已经说明，不同 source benchmark 到同一 target benchmark 的效果可能差别很大。例如 MMMU 与 GAOKAO-MM 作为源域时，在 CMMMU、MMMU-Pro、MathVista 上产生的结果明显不同。

因此，不应该固定只使用一个 source graph，而应该在没有目标标签的前提下，估计目标域更像哪些源域。

## 6.2 使用“回答拓扑”而不是原始答案文本

对每个 Query 构造与答案内容无关的结构向量：

[
z(q)=
[
\text{簇数},
\text{排序后的簇比例},
\text{分歧熵},
\text{最大簇与次大簇间隔},
\text{每簇覆盖的模型家族数},
\text{专家指纹均值/方差},
\text{不确定性分位数},
\text{缺失率}
].
]

这里不能跨 benchmark 直接比较答案字符串：

- MMMU-Pro 可能是 A–J；
- MathVista 可能是数字或开放答案；
- CMMMU 又可能有不同格式。

只能比较“回答如何分组”的拓扑结构。

## 6.3 估计目标域属于哪些源环境

假设有 (E) 个源环境，其回答拓扑分布为：

[
P_1(z),P_2(z),\ldots,P_E(z).
]

只使用目标域无标签输出，求：

# [ \pi^*

\arg\min_{\pi\in\Delta^E}
\operatorname{MMD}^2
\left(
P_T(z),
\sum_e\pi_eP_e(z)
\right)
+
\lambda|\pi|_2^2.
]

然后组合不同源环境的纠错图或训练样本：

# [ R_T

\sum_e
\pi_e^*R^{(e)}.
]

或者在训练 CPI-Selector 时，对源环境 episode 使用 (\pi_e^*) 重采样。

## 6.4 支持域检测

还可以计算：

# [ D_T

\min_{\pi\in\Delta^E}
\operatorname{MMD}
\left(
P_T,\sum_e\pi_eP_e
\right).
]

若 (D_T) 很大，说明目标域不在现有源 benchmark 的近似凸包内。此时系统不应盲目迁移，而应：

- 回退最佳单模型；
- 使用最保守的稳定图；
- 或触发少量目标域标注。

这使方法不只是“适应”，还可以检测什么时候不应该适应。

## 6.5 新颖性边界

“未知源域混合下进行模型路由”已经有 NeurIPS 2025 工作，因此 mixture adaptation 本身不是新的。([NeurIPS Proceedings](https://proceedings.neurips.cc/paper_files/paper/2025/file/39c60dda48ebf0a2e5dda52ce08eb5c8-Paper-Conference.pdf))

可能的新颖点是：

> 使用多专家已生成回答的、与标签值和模态无关的拓扑分布，估计目标 benchmark 的源环境组合，并迁移 post-hoc rescue policy。

## 6.6 必要实验

- uniform source mixture；
- 只用一个最佳 source；
- 根据 Query embedding 选择 source；
- 根据答案拓扑选择 source；
- 使用目标标签计算的 oracle source mixture，仅作为上界；
- MMD、domain classifier density ratio、Sinkhorn OT 三种实现；
- transductive：使用完整目标无标签集；
- online：只使用目标数据前缀，不能看到未来 Query；
- 目标不在源域支持内时的回退测试。

------

# 七、思路四：Latent Failure Archetypes——用失败原型替代 KNN

Improve5 当前根据输出状态检索约 32 个源域历史样本。

KNN 的问题是：

- 高维输出状态容易稀疏；
- 专家池变化后距离定义失效；
- 很难解释某一组近邻为什么代表同一种失败机制；
- 现有文献已经表明简单 KNN 是强基线，因此 KNN 本身不能作为新贡献。([ACL Anthology](https://aclanthology.org/2026.eacl-srw.22.pdf))

可以改成少量离散失败原型：

[
z_r\in{1,\ldots,K}.
]

例如学习：

[
P(Y_{rm}=1\mid z_r=k),
]

以及：

[
P(\text{answer topology}_r\mid z_r=k).
]

目标域中根据当前回答结构推断：

[
P(z_q=k\mid\text{current topology}),
]

再计算：

# [ P(Y_{qm}=1)

\sum_k
P(Y_{qm}=1\mid z_q=k)
P(z_q=k\mid\text{topology}_q).
]

可能得到的失败原型包括：

- 强模型孤立、多个弱模型形成正确共识；
- 全部专家高度分歧；
- 同一家族形成错误多数；
- 数学专家和通用专家分裂；
- 所有模型低置信度；
- 强模型与唯一独立家族一致。

这可以用：

- Bernoulli mixture；
- logistic PCA；
- NMF；
- variational EM；

先做低复杂度实现。

该方向与 IRT、latent annotator model 有邻近关系，适合作为解释性方法或 CPI-Selector 的分析工具，不建议单独作为最强 Claim。

------

# 八、思路五：Repair-Probe Coreset——为新专家选择最有信息量的 benchmark 探针

原 Subject-Level Bench-CoE 的扩展性优势是，新专家出现后可以更新 subject-expert mapping，而不需要重训 subject classifier。

但 Improve5/6 和前面的新方法都需要知道新专家的：

- 全局能力；
- subject 能力；
- 错误相关性；
- 纠错关系；
- latent fingerprint。

若每增加一个模型，都必须重新运行完整 benchmark，成本仍然很高。

可以研究：

> 为接入一个新模型，最少需要评测哪些 benchmark 问题，才能恢复它对下游回答选择真正有用的指纹？

定义候选探针集 (P)，优化：

# [ F(P)

\log\det
\left(
\epsilon I+
\sum_{r\in P}
\phi_r\phi_r^\top
\right)
+
\lambda\operatorname{MotifCoverage}(P).
]

其中 (\phi_r) 不只是 Query embedding，而应编码：

- 当前问题能够区分哪些专家；
- 是否覆盖某条稳定救援边；
- 是否覆盖某个失败原型；
- 是否能区分同家族专家；
- 是否处于高 oracle gap 区域。

实验可以直接利用完整缓存模拟：

- 只给新专家 1% benchmark 结果；
- 5%；
- 10%；
- 25%；
- 100%。

比较：

- random；
- subject-stratified；
- embedding k-center；
- IRT Fisher information；
- proposed repair-policy-preserving coreset。

通用模型路由已经研究过代表性 prompts 和未见模型接入，所以不能声称首次使用探针描述新模型。([arXiv](https://arxiv.org/abs/2502.08773))

较安全的新颖点是：

> 探针集不是为了恢复模型总体准确率，而是为了保持下游 rescue policy 和答案选择决策。

------

# 九、思路六：Higher-order Rescue Motifs——高阶失败模式

可以构造如下 motif：

[
(i,j)\rightarrow k:
\quad
P(k\text{正确}\mid i,j\text{同时错误})
]

或：

[
(\text{InternVL孤立},
\text{三个不同家族同答})
\rightarrow
\text{多数答案正确}.
]

还可以加入答案关系：

[
P
\left(
c\text{正确}
\mid
a_i\neq c,,
a_j=c,,
a_k=c,,
family(j)\neq family(k)
\right).
]

只保留：

- 支持样本数足够；
- 在多个 source environment 中同号；
- Beta-Binomial 下界为正；
- 不被低阶边解释的 motif。

但这个方向的文献风险较高。已有工作已经研究 LLM 高阶信息、模型相关性和 structured message passing。([arXiv](https://arxiv.org/html/2510.01499v1))

因此更适合：

- 作为 CPI-Selector 的可解释基线；
- 作为 DCRG 的二阶扩展；
- 而不是独立主方法。

------

# 十、思路七：Sequential Rescue Acquisition——逐步追加专家

当前 Improve5/6 默认所有回答已经生成，推理成本约等于运行全部专家。源域和目标域预测缓存已经存在，因此可以先完全离线模拟逐步调用，不需要立即重新运行模型。

流程可以是：

1. 先调用源域确定的最佳单模型；
2. 根据已有回答状态，预测下一个最有价值专家；
3. 获得该专家缓存回答；
4. 更新答案簇后决定停止或继续；
5. 达到置信度阈值后输出。

定义状态 (s_t)，候选专家 (j) 的价值：

# [ \operatorname{VOI}(j\mid s_t)

## \mathbb E \left[ \max_cP(c\text{正确}\mid s_t,a_j) \right]

## \max_cP(c\text{正确}\mid s_t)

\lambda\operatorname{cost}_j.
]

当：

[
\max_j\operatorname{VOI}(j\mid s_t)\leq0
]

时停止。

应报告：

- accuracy；
- 平均调用模型数；
- P50/P90/P95 调用数；
- accuracy–calls 曲线；
- 相同平均调用数下的 fixed-(k)；
- 随机追加；
- 按全局准确率追加；
- 不确定性 cascade；
- oracle sequential policy。

序列路由、cascade 和 adaptive consensus 已经是拥挤方向，因此该模块更适合作为准确率主方法之后的部署扩展，而不是第一主创新。([arXiv](https://arxiv.org/html/2603.04445v2))

------

# 十一、最推荐的论文主线

建议暂定研究问题为：

> **Benchmark-Conditioned Response Selection under Domain Shift and Model-Pool Shift**

中文可以概括为：

> **面向数据分布变化与专家池变化的基准驱动回答选择**

分阶段验证：

### 阶段 A：DCRG

先回答：

> 去除专家全局能力和题目难度后，Improve6 的纠错边是否仍然存在？

若 DCRG 不能稳定胜过原始 (C)，说明 RepairChain 的收益主要来自强模型中心性，不宜继续做图传播。

### 阶段 B：CPI-Selector

回答：

> 通过专家池干预训练，能否对专家增删、复制、替换和缺失保持稳定？

这是最可能形成独立主贡献的阶段。

### 阶段 C：TopoMix

回答：

> 无标签目标回答拓扑能否判断应从哪个 benchmark 迁移？

仅在存在至少两个可兼容源环境时实施。

### 阶段 D：成本扩展

对阶段 B/C 的最佳选择器增加 sequential acquisition。

不要一开始把四个模块全部拼起来。只有当某模块在 source leave-one-domain-out 上产生独立增益时，才进入组合实验。

------

# 十二、实验纪律

由于 Improve5 和 Improve6 是在观察多个 OOD 结果后筛选出来的，已经看过的目标数据集应当视为 **development OOD**，不能再承担最终无偏测试集的角色。

正式实验应遵守：

1. 所有源域统计使用 cross-fitting；
2. 目标标签不能进入特征、超参数、阈值和方法选择；
3. 当前已经反复查看结果的 MathVista、CMMMU、MMMU-Pro 等作为开发集；
4. 最终至少保留一个未参与任何设计的新 benchmark 或 blind split；
5. 先冻结代码、配置、权重和 Git commit，再运行最终目标；
6. 所有尝试过的方法都记录，包括负结果；
7. 不按目标结果选择随机种子。

核心指标应包括：

# [ \text{RescueRate}

\frac{
\#(\text{best single错，方法对})
}{N},
]

# [ \text{HarmRate}

\frac{
\#(\text{best single对，方法错})
}{N},
]

# [ \text{NetUplift}

\text{RescueRate}-\text{HarmRate},
]

# [ \text{SwitchPrecision}

\frac{#\text{Rescue}}
{#\text{Rescue}+#\text{Harm}},
]

# [ \text{OracleGapClosed}

\frac{
Acc_{\mathrm{method}}-Acc_{\mathrm{best}}
}{
Acc_{\mathrm{oracle}}-Acc_{\mathrm{best}}
}.
]

同时报告：

- 每数据集结果；
- macro average；
- worst-domain delta；
- paired bootstrap 95% CI；
- exact McNemar；
- 多方法比较的 Holm correction；
- 模型池扰动后的准确率下降；
- 缺失输出鲁棒性；
- 运行时间、显存和额外调用成本。

当前案例中，简短回答的 (U_m(q)) 全部为 0，因此新方法必须在没有可靠 uncertainty 的情况下也能工作，不能把全部提升建立在 (U_m) 上。

------

# 十三、Codex CLI 的使用方式

建议在仓库根目录启动 Codex，一次只粘贴一个阶段 Prompt。Codex CLI 可以直接检查仓库、修改文件和运行本地测试；官方建议重要任务前后建立 Git checkpoint。`AGENTS.md` 会在 Codex 工作前自动加载，因此适合固化数据泄漏、实验输出和验证规则。([OpenAI Developers](https://developers.openai.com/codex/cli))

建议先建立分支：

```bash
cd /你的/Bench-CoE仓库
git status
git switch -c research/pool-shift-response-selection
codex
```

不要一开始授权 Codex 覆盖原实验目录。所有新产物写入独立目录。

------

# 十四、分步 Prompt 0：仓库、缓存与泄漏审计

```text
你当前位于 Bench-CoE 项目仓库根目录。

当前阶段只做审计和计划：
- 不实现任何新算法；
- 不修改现有 Improve5、Improve6 和 Bench-CoE 结果；
- 不重新运行任何大模型；
- 不覆盖 outputs 中的任何现有文件；
- 除审计文档外，不修改源代码。

首先读取当前目录及父子目录中适用的 AGENTS.md / AGENTS.override.md。
然后检查 git status、当前 commit、未提交修改，但不要执行 git commit、reset、checkout 或 clean。

重点检查以下已知或同名路径；若路径不存在，搜索实际位置，不要伪造：
- bench_coe/configs/expert_pools.json
- outputs/bench_coe/ALL_EXPERIMENTS_RESULTS.csv
- outputs/bench_coe/mmmu_pro_source_improve5/
- outputs/bench_coe/mmmu_pro_source_improve6/
- outputs/multimodal_babyvision_models/mmmu_pro/standard_10_options/test
- outputs/multimodal_babyvision_models/mathvista/testmini
- choices_by_method.json
- Improve5 / DARE 实现
- Improve6 / RepairChain 实现

输出 docs/innovation/00_REPO_AND_LEAKAGE_AUDIT.md，至少包含：

1. 仓库结构和相关代码入口；
2. 每个缓存文件的真实路径、格式、规模和关键字段；
3. question_id、dataset、split、subject、model、raw_answer、
   normalized_answer、correctness、uncertainty、judge、cost 等字段是否存在；
4. gold label 和 correctness 在代码中的读取位置；
5. Improve5 和 Improve6 的完整调用链；
6. 当前答案归一化和答案等价判断规则；
7. uncertainty 的真实定义、缺失率和数值分布；
8. 每个数据集可用专家列表及专家池交集；
9. 可作为源域和目标域的所有 dataset/split；
10. 已经被用于方法开发或权重选择的数据集；
11. 可能的数据泄漏、自邻居泄漏、target-label leakage、judge leakage；
12. 哪些实验可以完全使用缓存完成；
13. 哪些实验需要重新运行模型；
14. 当前项目实际可用的测试、lint 和运行命令；
15. 阻塞后续实现的缺失信息。

同时生成 docs/innovation/experiment_manifest.yaml，记录：
- dataset；
- split；
- modality；
- source_or_target；
- development_or_locked_test；
- expert list；
- cache path；
- label path；
- judge；
- answer type；
- sample count。

所有结论必须附真实文件路径和代码行号。
无法确认的内容标记 UNKNOWN，不要自行推断。
完成后停止，不要开始实现。
```

------

# 十五、分步 Prompt 1：建立实验契约和 AGENTS.md

```text
读取：
- docs/innovation/00_REPO_AND_LEAKAGE_AUDIT.md
- docs/innovation/experiment_manifest.yaml
- 当前适用的 AGENTS.md

当前任务是建立 innovation 实验的工程约束，不实现新方法。

若仓库已有根目录 AGENTS.md，不要覆盖。
优先在 bench_coe/innovation/AGENTS.md 创建作用域明确的规则；
若最终代码目录不同，则把 AGENTS.md 放在能覆盖所有 innovation 代码的最近公共目录。

AGENTS.md 必须规定：

1. 禁止使用目标域 gold/correctness 训练、调参、选阈值、选方法或选种子；
2. target labels 只能由最终 evaluator 读取；
3. 所有源域局部统计、正确率、图边和训练特征必须使用 out-of-fold 或严格分离的 split；
4. 不覆盖已有 outputs；
5. 新产物统一写入 outputs/bench_coe/innovation/<run_id>/；
6. 每次实验保存 config、seed、git commit、命令、环境、输入缓存 hash；
7. 所有随机算法必须支持显式 seed；
8. 所有方法输出逐题 prediction、selected answer cluster、selected expert、
   scores、fallback reason 和 observable features；
9. 负结果也必须进入总报告；
10. 禁止根据目标结果修改默认参数；
11. 先运行 unit test 和小规模 smoke test，再运行全量；
12. 不在代码中静默硬编码专家名称、数据集规模或文件路径；
13. 专家 family 映射必须放在可审查配置中；
14. 不允许比较跨 Query 或跨 benchmark 的原始答案字符串；
15. answer cluster 只能在单个 Query 内构建。

创建：
- docs/innovation/EXPERIMENT_PROTOCOL.md
- bench_coe/innovation/ 或符合仓库风格的等价目录
- configs/innovation/
- tests/innovation/
- outputs/bench_coe/innovation/.gitkeep（仅当仓库惯例允许）

EXPERIMENT_PROTOCOL.md 要定义：
- development source；
- source validation；
- development OOD；
- locked final test；
- baseline；
- rescue/harm/net uplift；
- oracle gap closed；
- paired bootstrap；
- McNemar；
- Holm correction；
- pool-shift stress tests；
- GO/NO-GO 标准。

不要实现算法。
运行最小格式检查并展示 git diff 摘要。
完成后停止。
```

------

# 十六、分步 Prompt 2：统一缓存数据层与标签防火墙

```text
基于审计和 EXPERIMENT_PROTOCOL，实现 innovation 实验的统一数据层。

目标：
任何选择器的 fit() 和 predict() 接口都不能直接访问目标域 gold 或 correctness。

实现符合仓库风格的模块，建议包括：

- CanonicalPredictionRecord
- ObservableQueryBatch
- SourceTrainingLabels
- EvaluationLabels
- ExpertPool
- AnswerCluster
- DatasetManifest
- CacheAdapter

统一记录至少包含：
- dataset
- split
- question_id
- subject
- modality
- expert_id
- expert_family
- raw_answer
- normalized_answer
- per_query_cluster_id
- uncertainty
- valid_output
- missing_reason
- source_global_accuracy（仅由 source 构建）
- source_fingerprint
- inference_cost（缺失时允许 null）
- correctness：不得出现在 ObservableQueryBatch 中

关键约束：

1. answer cluster 只在同一个 question_id 内生成；
2. 不把 A、B、C、数字2等原始值跨数据集比较；
3. target ObservableQueryBatch 中不得包含 gold/correctness；
4. evaluator 必须在 prediction 文件落盘后，才能单独读取 EvaluationLabels；
5. source statistics 与 target features 使用不同对象；
6. 专家池不一致时支持 mask，而不是填入虚假答案；
7. 缺失输出作为一等状态处理；
8. family 映射保存在 YAML/JSON 配置中，并生成待人工复核列表。

至少编写以下测试：

- target feature object 不含 correctness；
- 训练函数尝试读取 target label 时测试失败；
- expert 顺序打乱后 answer clusters 等价；
- 同一答案不同格式能按已有规范归一化；
- 不同 Query 的相同答案不会被合并；
- 缺失输出不会被当成错误答案簇；
- 不同专家池可以批处理；
- 数据加载具有确定性；
- 缓存重复 question_id 能被检测；
- source/target question_id 重叠会触发警告或失败。

先对每个缓存抽取不超过 20 条做 smoke test。
不要运行全量方法实验。

输出：
- docs/innovation/01_CANONICAL_DATA_LAYER.md
- 单元测试结果
- 实际字段覆盖率表
- 尚无法构造的字段列表

完成后停止。
```

------

# 十七、分步 Prompt 3：基线复现和统一评测器

```text
使用统一数据层实现或包装以下基线：

1. source-only Best Single；
2. Majority Vote；
3. source-accuracy Weighted Vote；
4. family-balanced vote；
5. 原 Improve5 / DARE；
6. 原 Improve6 / RepairChain；
7. output-profile KNN only；
8. global + local competence；
9. oracle-any-expert，仅作为评测上界；
10. random expert 和 random answer cluster sanity checks。

Best Single 必须只根据源域训练部分确定，不能查看目标准确率。

统一评测器必须输出：

- accuracy
- delta_vs_source_best_single
- rescue_count / rescue_rate
- harm_count / harm_rate
- unchanged_correct
- unchanged_wrong
- switch_count / switch_rate
- switch_precision
- net_uplift
- oracle_accuracy
- oracle_gap_closed
- per_subject accuracy
- per_family selection rate
- expert selection entropy
- answer-cluster selection entropy
- missing-output impact
- paired bootstrap 95% CI
- exact McNemar p-value

所有逐题结果保存为 JSONL 或 Parquet。
汇总保存为 CSV 和 Markdown。

先复现现有 Improve5/6 的一个已知小案例和至少一个完整数据集。
若结果与已有 choices_by_method.json 不一致：
- 不要调整代码去迎合结果；
- 输出第一个不一致样本；
- 比较答案归一化、模型顺序、权重、KNN、uncertainty 和 tie-breaking；
- 说明差异来源。

创建：
- docs/innovation/02_BASELINE_REPRODUCTION.md
- outputs/bench_coe/innovation/baseline_reproduction/
- 可复用的 CLI，例如：
  python -m bench_coe.innovation.run --config ...

运行测试和一个小规模 smoke experiment。
完成后停止。
```

------

# 十八、分步 Prompt 4：DCRG 设计审查，不写代码

```text
当前任务只设计 DCRG：
Difficulty-Adjusted Cross-Environment Rescue Graph。
不要修改代码。

读取：
- 00_REPO_AND_LEAKAGE_AUDIT.md
- EXPERIMENT_PROTOCOL.md
- 01_CANONICAL_DATA_LAYER.md
- 02_BASELINE_REPRODUCTION.md
- 原 Improve6 实现

输出 docs/innovation/03_DCRG_DESIGN.md。

设计必须包括：

1. 明确说明原始 C_ij=P(j correct | i wrong) 的三个混杂：
   expert strength、item difficulty、environment composition。

2. 提供至少两种 cross-fitted expected-correctness 模型：
   A. 简单 logistic/Rasch 风格；
   B. low-rank logistic matrix factorization。
   默认先实现数据量更稳健的一种。

3. 定义：
   E_rj = Y_rj - p_hat_rj
   R_ij^e = E[E_rj | Y_ri=0, environment=e]

4. item difficulty proxy 在估计 i→j 时不得直接使用 i 和 j 的标签产生循环定义；
   给出 leave-i-j-out difficulty 的实现。

5. 定义跨环境稳定边：
   - lower confidence bound；
   - sign consistency；
   - minimum support；
   - hierarchical shrinkage；
   - negative edge 是否保留。

6. 明确环境候选：
   dataset、subject、预设 fold。
   若只有一个真正 source dataset，必须区分“跨 subject 稳定”与“跨 dataset 稳定”，
   不得把前者夸大为 domain invariance。

7. 当前 Query 的失败概率必须由 observable features 进行 OOF 学习，
   不得使用手工固定系数作为唯一版本。

8. 默认只实现一跳。
   两跳只能作为消融，不能默认假定有益。

9. 优先选择 answer cluster，而不是在给出相同答案的模型间做无意义排序。

10. 列出数组形状、伪代码、复杂度、缓存需求、单元测试和失败条件。

11. 预注册消融：
   raw C
   C-G
   residual R
   stable residual R
   no difficulty adjustment
   no expert-strength adjustment
   pooled vs environment-stable
   self-loop on/off
   real vs degree-preserving randomized graph
   one-hop vs two-hop

12. 定义 GO 标准：
   只允许使用 source validation / source leave-one-environment-out；
   不能根据目标结果决定是否继续。

完成设计文档后停止。
```

------

# 十九、分步 Prompt 5：实现和验证 DCRG

```text
按照 docs/innovation/03_DCRG_DESIGN.md 实现 DCRG。
不得偏离设计；必须变更时先在文档中记录原因。

实现要求：

1. 所有 expected-correctness 都使用 out-of-fold prediction；
2. 每条 rescue edge 保存：
   raw support、raw C、expected baseline、residual mean、
   standard error、LCB、environment signs、stable flag；
3. 默认 C_ii=0；
4. 当前失败估计器先使用可解释的 logistic regression；
5. observable features 至少包含：
   answer support、answer partition entropy、cluster margin、
   family breadth、valid/missing、可用 uncertainty、source fingerprint；
6. uncertainty 缺失或全零时方法仍能运行；
7. 不在默认版本中使用 target query embedding；
8. 不使用 target labels 选择阈值；
9. 输出 expert score 和 answer-cluster score；
10. 所有 tie-breaking 必须确定且记录。

测试至少包括：

- OOF 行不能参与自身统计；
- leave-i-j-out difficulty 不包含 i/j correctness；
- 随机图消融真的保持规定的度或边分布；
- self-loop 为零；
- 相同 seed 完全复现；
- target labels 不可访问；
- source fold 发生重叠时失败；
- 极少失败样本时正确回缩；
- 所有专家从不失败或从不正确时不产生 NaN。

运行顺序：

A. 50题 smoke test；
B. 单个 source validation；
C. source leave-one-subject/environment-out；
D. 只有满足预注册 GO 标准才运行 development OOD。

输出：
- outputs/bench_coe/innovation/dcrg/<run_id>/
- docs/innovation/04_DCRG_RESULTS.md
- 全部 config、逐题结果、边表、消融表和统计检验。

不要根据 development OOD 重新调默认参数。
完成后停止。
```

------

# 二十、分步 Prompt 6：CPI-Selector 设计审查

```text
当前任务只设计 Counterfactual Pool-Intervention Answer-Cluster Selector，
简称 CPI-Selector。不要修改代码。

目标不是普通图聚合，而是：
训练一个能够处理可变专家池，并对专家顺序、复制、增删、家族变化和缺失输出
保持鲁棒的 post-hoc answer-cluster selector。

输出 docs/innovation/05_CPI_SELECTOR_DESIGN.md。

设计必须包括：

1. 输入对象：
   - 可变数量 expert tokens；
   - 可变数量 answer-cluster tokens；
   - expert-to-cluster incidence；
   - valid/missing mask；
   - source-only expert fingerprints。

2. expert fingerprint 候选：
   - source global accuracy；
   - subject accuracy vector；
   - correctness PCA/NMF embedding；
   - DCRG in/out rescue profile；
   - family；
   - cost。
   明确哪些为默认，哪些为消融。

3. 模型必须 permutation-invariant。
   优先设计不依赖 PyTorch Geometric 的 DeepSets / attention pooling 最小版本。

4. 不能直接使用原始 answer value 作为跨数据集特征。
   cluster ID 是每个 Query 内的关系标记，不具有跨 Query 语义。

5. 输出为每个 answer cluster 的 correctness score，
   不是固定维度的 expert classification head。

6. 训练 episode：
   - random expert subset；
   - leave-one-expert-out；
   - leave-one-family-out；
   - missing-output mask；
   - exact clone duplication；
   - same-family pseudo-clone；
   - known expert-pool swap。

7. exact clone duplication 的预测应保持不变。
   给出 clone-invariance loss 和 cluster 对齐方法。

8. 对 expert dropout 不强制输出完全一致，
   而是在每个子池上使用正确监督；
   防止错误地要求删除重要证据后仍保持相同决策。

9. 训练目标：
   supervised cluster CE/BCE
   + clone invariance
   + optional calibration
   + optional DCRG structural prior。

10. 先给出三种容量：
    - linear pooled baseline；
    - small DeepSets；
    - attention pooling。
    默认从最小模型开始。

11. 单元性质测试：
    permutation invariance；
    clone invariance；
    variable pool size；
    missing expert；
    one-cluster unanimous case；
    all-singleton disagreement；
    unseen expert fingerprint；
    arbitrary cluster relabeling。

12. 评测：
    normal target；
    random drop；
    family drop；
    duplicate experts；
    swap pool；
    missing judge；
    leave-one-model/family-out。

13. 明确与普通 GNN、structured message passing、family-weighted vote、
    universal pre-inference router 的区别。

14. 给出 GO/NO-GO 标准和预计显存、训练时间。

完成后停止。
```

------

# 二十一、分步 Prompt 7：实现 CPI-Selector

```text
严格按照 05_CPI_SELECTOR_DESIGN.md 实现 CPI-Selector。

工程要求：

1. 不修改原 Bench-CoE、Improve5、Improve6 代码；
2. 新代码位于 innovation 作用域；
3. 默认实现 linear pooled baseline 和 small DeepSets；
4. 只有 small DeepSets 在 source validation 有增益后才实现 attention pooling；
5. 不新增不必要的大型依赖；
6. 所有专家和 family 名称来自配置；
7. 支持不同 Query 上不同 expert mask；
8. 支持不同数量 answer clusters；
9. target labels 只能由 evaluator 使用；
10. 训练 episode 和干预随机种子可复现。

必须实现的干预：

- permutation；
- random expert dropout；
- leave-family-out；
- exact clone duplication；
- same-family pseudo-clone；
- missing output；
- known swap pools。

clone duplication 测试中：
同一输入加入一个与现有专家 answer、fingerprint 和 uncertainty 完全相同的 clone 后，
每个已存在答案簇的概率变化必须低于配置阈值。

训练和实验顺序：

A. 单元测试；
B. 100题 overfit sanity test；
C. source train/validation；
D. leave-one-subject-out；
E. leave-one-expert-out；
F. leave-one-family-out；
G. development OOD；
H. pool-shift stress test。

比较：
- Best Single
- Majority
- family-balanced vote
- output-profile KNN
- Improve5
- Improve6
- DCRG
- linear pooled cluster selector
- DeepSets without interventions
- DeepSets with each intervention
- full CPI-Selector

输出：
- docs/innovation/06_CPI_SELECTOR_RESULTS.md
- outputs/bench_coe/innovation/cpi_selector/<run_id>/
- 每个干预的独立消融；
- pool size 与性能曲线；
- clone sensitivity；
- family removal sensitivity；
- calibration curve；
- 逐题结果和错误案例。

不允许只报告最优干预组合。
完成后停止。
```

------

# 二十二、分步 Prompt 8：TopoMix 实现

```text
先读取 experiment_manifest.yaml 和统一缓存报告。

仅当满足以下条件时实现 TopoMix：
- 至少有两个可区分的 source environment；
- 每个环境具有足够共同专家或可用的 mask-aware fingerprint；
- 每个环境有足够样本；
- target 具有无标签专家输出。

若条件不满足，只输出 feasibility report，不使用目标标签制造伪 source environment。

实现目标：
仅使用目标域无标签 answer topology，估计目标域对应的 source mixture，
并重加权 DCRG 或 CPI-Selector 的 source information。

构造 pool-invariant topology z(q)，至少包含：
- number of valid experts
- number of answer clusters
- sorted cluster proportions
- disagreement entropy
- top1-top2 cluster margin
- family breadth per cluster
- fingerprint mean/variance per cluster
- uncertainty quantiles
- missingness
- DCRG structural summaries

禁止使用：
- target gold
- target correctness
- raw answer identity across datasets
- target performance for method selection

实现至少两种 target weighting：
1. constrained MMD source-mixture；
2. domain-classifier density ratio。

可选第三种：
3. entropic optimal transport。

同时实现：
- uniform source；
- nearest single source；
- source-size weighting；
- oracle target-label mixture，仅作为 evaluator 中的诊断上界，不能进入选择流程。

提供两种协议：
- transductive：使用完整 target 无标签 topology；
- online：处理第 t 个 Query 时只能使用 1..t-1 的 topology。

估计 support distance。
当 target topology 超出源 mixture 支持时，回退：
- stable DCRG；
- source-best single；
- 或预先指定的保守 CPI policy。

所有 target mixture 权重必须在读取 target labels 前写入并 hash 锁定。

输出：
- docs/innovation/07_TOPOMIX_RESULTS.md
- source mixture weights
- support-distance diagnostics
- transductive/online 对比
- uniform/nearest/oracle 上界对比
- target-label firewall 测试结果

完成后停止。
```

------

# 二十三、分步 Prompt 9：Repair-Probe Coreset

```text
当前任务研究新专家接入时需要评测多少 source benchmark 样本。

不得重新运行模型。
使用已有完整缓存模拟只观察一个子集 P 的结果。

先选择当前 source-validation 表现最佳且已经冻结的 selector，
目标是在只观察新专家少量 benchmark 回答时恢复其：
- source fingerprint；
- stable rescue profile；
- 下游 answer-selection utility。

实现基线：
- random
- subject-stratified random
- query-embedding k-center
- output-topology k-center
- IRT/Fisher information
- uncertainty sampling

提出 Repair-Probe Coreset：
目标函数同时覆盖：
- expert discrimination
- stable rescue edges
- failure archetypes
- answer-topology motifs
- downstream policy sensitivity

优先实现可复现的 greedy submodular 或 D-optimal 版本。

模拟预算：
1%、2%、5%、10%、20%、50%、100%。

测试协议：
- leave-one-expert-out onboarding；
- leave-one-family-out onboarding；
- Qwen3.5 与 Qwen3-VL replacement；
- 不同 source benchmark；
- 不同 pool sizes。

指标：
- fingerprint reconstruction error
- rescue-edge reconstruction error
- selected-answer agreement with full-information selector
- final accuracy
- oracle-gap closed
- probe cost
- performance per 100 probe questions

必须区分：
“恢复总体准确率”和“保持下游选择策略”。
本方法的目标是后者。

输出：
- docs/innovation/08_REPAIR_PROBE_CORESET.md
- 每个预算的完整曲线
- 选中 probe 列表
- probe subject/motif 分布
- 与 random/IRT/k-center 的比较

完成后停止。
```

------

# 二十四、分步 Prompt 10：离线逐步调用模拟

```text
使用完整 expert-output cache 构建 Sequential Rescue Acquisition 离线模拟器。
当前阶段不得真实调用模型。

状态包括：
- 已调用专家集合；
- 已观察回答簇；
- 当前 cluster scores；
- expert fingerprints；
- missing mask；
- accumulated cost。

动作包括：
- 调用一个尚未观察的专家；
- STOP。

所有策略从 source-only best single 开始。

实现基线：
- fixed-k global-accuracy order
- random order
- family-diverse order
- uncertainty cascade
- disagreement threshold
- oracle sequential policy

实现一个 myopic value-of-information policy：
预测调用候选专家后最终正确率的期望增量，
减去 lambda * cost。
当所有候选 VOI<=0 时停止。

训练时：
- 从 source full-output cache 随机 mask 未观察专家；
- target labels 不参与训练或停止阈值选择；
- cost 缺失时先以每次调用1单位报告，
  不要伪造真实美元或延迟。

报告：
- accuracy
- average calls
- P50/P90/P95 calls
- accuracy-call Pareto curve
- area under accuracy-call curve
- rescue/harm
- stop error
- unnecessary-call rate
- per-dataset and worst-domain result

比较：
- 全专家 CPI/DCRG/TopoMix
- source best single
- fixed 2/3/4 expert
- proposed sequential policy

输出：
- docs/innovation/09_SEQUENTIAL_RESCUE.md
- 可重放的逐题 action trace
- accuracy-cost CSV
- 所有策略的同预算比较

完成后停止。
```

------

# 二十五、分步 Prompt 11：冻结方法并运行最终实验

```text
当前任务是生成最终、不可回看调参的评测包。

首先读取所有 innovation 结果。
只允许根据 source validation、source leave-one-environment-out 和预注册 GO 标准
选择最终方法和参数。
不得根据 development OOD 选择权重、架构、seed 或阈值。

创建：
- configs/innovation/final_frozen.yaml
- docs/innovation/FINAL_EVALUATION_PLAN.md

记录：
- git commit
- working tree status
- Python/CUDA/PyTorch versions
- input cache hashes
- source split hashes
- final method
- final hyperparameters
- seeds
- locked target datasets
- statistical tests
- stopping rules
- expected output paths

在运行 target evaluator 前：
1. 生成所有 target predictions；
2. 写入 prediction 文件；
3. 计算文件 hash；
4. 锁定 config；
5. 确认训练和预测日志中未读取 target labels。

然后才允许 evaluator 读取 target labels。

运行所有预注册 seed。
生成：

- outputs/bench_coe/innovation/final/
- FINAL_RESULTS.csv
- PER_QUERY_RESULTS.parquet 或 jsonl
- RESCUE_HARM_TABLE.csv
- POOL_SHIFT_RESULTS.csv
- STATISTICAL_TESTS.csv
- docs/innovation/FINAL_INNOVATION_REPORT.md

报告必须包含：
- 所有数据集；
- 所有 baseline；
- 所有负结果；
- 每个数据集相对 source-best-single 的差值；
- 相对 Improve5/6 的差值；
- paired CI；
- McNemar + Holm；
- worst-domain；
- pool-shift；
- missing-output；
- source-data budget；
- compute/cost；
- 失败案例分类；
- 哪些结论得到支持；
- 哪些结论只在单一数据集或 seed 上成立；
- GO/NO-GO。

禁止修改冻结配置后重新运行同一 locked target。
完成后停止。
```

------

# 二十六、分步 Prompt 12：独立敌对代码审计

Codex CLI 目前支持将独立任务委派给 subagents，因此最终实现后可以让不同代理分别检查泄漏、统计和工程问题。([OpenAI Developers](https://developers.openai.com/codex/agent-configuration/subagents))

```text
对当前 innovation 实现执行只读敌对审计。
不要先修改代码。

请使用至少三个相互独立的 subagents：

Agent A：数据泄漏审计
- target labels 是否进入 fit/predict；
- source 样本是否参与自身 KNN、图边、fingerprint 或 OOF prediction；
- 是否根据 development/target 结果选参数；
- answer normalization 是否跨 Query 泄漏；
- judge 输出是否与 gold 混用。

Agent B：统计与实验审计
- baseline 是否公平；
- Best Single 是否错误地使用目标结果；
- bootstrap 是否 paired；
- McNemar 是否使用对齐样本；
- 多重比较是否正确；
- seed 和 split 是否完整；
- 是否存在只报告最好结果；
- oracle 是否被误用于训练。

Agent C：工程与复现审计
- 路径硬编码；
- 覆盖旧输出；
- nondeterminism；
- expert/family 顺序依赖；
- clone/permutation invariance；
- missing-output handling；
- config/hash/commit 是否保存；
- CLI 是否可从干净环境执行。

可选 Agent D：方法机制审计
- DCRG 是否真正去除了 difficulty/global-strength；
- CPI-Selector 增益是否只是模型容量；
- TopoMix 是否意外读取 target labels；
- 两跳或图结构是否只是中心性；
- pool intervention 是否与测试扰动一致。

主代理汇总为：
docs/innovation/ADVERSARIAL_AUDIT.md

每个问题给出：
- severity：BLOCKER/HIGH/MEDIUM/LOW
- 文件和行号
- 可复现触发方式
- 对实验结论的影响
- 最小修复方案

先输出报告，不要修改。
等待下一条指令后再修复 BLOCKER/HIGH。
```

------

# 二十七、实际执行顺序

最合理的实施顺序是：

| 顺序 | 工作                             | 继续条件                                   |
| ---- | -------------------------------- | ------------------------------------------ |
| 1    | Prompt 0–3：审计、数据层、基线   | 必须完整复现已有结果                       |
| 2    | Prompt 4–5：DCRG                 | source LODO 稳定优于原始 RepairChain       |
| 3    | Prompt 6–7：CPI-Selector         | 在正常池和 pool-shift 上均有收益           |
| 4    | Prompt 8：TopoMix                | 至少两个真实源环境且无标签权重优于 uniform |
| 5    | Prompt 9：Probe Coreset          | 主选择器已经有效                           |
| 6    | Prompt 10：Sequential            | 需要恢复成本优势时实施                     |
| 7    | Prompt 11–12：冻结评测和敌对审计 | 投稿前必须完成                             |

最优先的判断标准不是单一平均准确率，而是：

[
\boxed{
\text{OOD 准确率提高}
+
\text{worst-domain 不退化}
+
\text{专家池替换后仍稳定}
}
]

其中最有希望形成清晰论文贡献的是：

> **CPI-Selector 的模型池反事实干预训练，配合 DCRG 的去混杂稳定救援先验；TopoMix 作为无标签目标迁移模块。**

这比再提出一个局部正确率、agreement、全局准确率和图分数的固定加权公式，更可能同时解决当前论文的泛化失败、强模型塌缩和专家池变化问题。